<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:orientation = "vertical"
    tools:context=".MainActivity"
    tools:ignore="ExtraText">
    <Spinner
        android:id="@+id/joke"
        android:layout_width="500dp"
        android:layout_height="48dp"
        android:layout_marginTop = "40dp"
        android:layout_gravity = "center"
        android:layout_margin = "16dp"
        android:theme="@style/AlwaysEnableScrollBar"
        android:entries="@array/jokes"/>

    <Button
        android:id="@+id/show_punchLine"
        android:layout_width="264dp"
        android:layout_height="wrap_content"
        android:layout_gravity = "center"
        android:layout_margin = "5dp"
        android:text="@string/show_PunchLine"
        android:onClick="onClickShowPunchLine"/>

    <TextView
        android:id="@+id/punchLines"
        android:layout_width="357dp"
        android:layout_height="259dp"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:fontFamily="sans-serif"
        android:padding="40dp"
        android:text="@string/PunchLines"
        android:textSize="20sp" />

</LinearLayout>